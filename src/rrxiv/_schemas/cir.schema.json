{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rrxiv.com/schema/v0/cir.schema.json",
  "title": "Canonical Intermediate Representation (CIR)",
  "description": "The full structured representation of a rrxiv paper. A Paper's metadata plus its derived structure (sections, claims, citations, figures, annotations). Extracted from TeX/Typst source by the rrxiv parser. The unit of agent-readable knowledge in the rrxiv corpus.",
  "version": "0.2.0",
  "type": "object",
  "required": ["rrxiv_version", "id", "version", "title", "authors", "abstract", "submitted_at", "license", "source"],
  "properties": {
    "rrxiv_version": {
      "type": "string",
      "description": "Version of the rrxiv protocol this CIR conforms to. SemVer.",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "examples": ["0.1.0"]
    },
    "id": {
      "type": "string",
      "description": "Stable, canonical identifier for this paper. Format TBD; for v0 use UUIDv7. Once assigned, NEVER changes.",
      "examples": ["01923f8e-5b2a-7c4d-9e1f-3a2b1c0d4e5f"]
    },
    "version": {
      "type": "string",
      "description": "Paper revision. The same paper may have v1, v2, etc. Each version is immutable once submitted.",
      "pattern": "^v[0-9]+$",
      "examples": ["v1", "v2"]
    },
    "previous_version": {
      "type": ["string", "null"],
      "description": "ID of the previous version of this paper, if any. Null for v1."
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500
    },
    "authors": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "paper.schema.json#/$defs/Author" }
    },
    "abstract": {
      "type": "string",
      "description": "Plain-text abstract. Math should be expressed in LaTeX inline."
    },
    "submitted_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when this version was submitted to rrxiv. Set by server, immutable."
    },
    "license": {
      "type": "string",
      "description": "SPDX license identifier for the paper content. rrxiv recommends CC-BY-4.0.",
      "examples": ["CC-BY-4.0", "CC-BY-SA-4.0", "CC0-1.0"]
    },
    "topics": {
      "type": "array",
      "description": "Topic IDs from the rrxiv controlled vocabulary. May be added or refined post-submission via annotations.",
      "items": { "type": "string" }
    },
    "source": {
      "$ref": "paper.schema.json#/$defs/Source"
    },
    "sections": {
      "type": "array",
      "items": { "$ref": "section.schema.json" }
    },
    "claims": {
      "type": "array",
      "description": "Claims extracted from this paper. May be added post-hoc by annotators or agents, subject to author confirmation for canonical status.",
      "items": { "$ref": "claim.schema.json" }
    },
    "citations": {
      "type": "array",
      "items": { "$ref": "citation.schema.json" }
    },
    "figures": {
      "type": "array",
      "items": { "$ref": "figure.schema.json" }
    },
    "annotations": {
      "type": "array",
      "description": "Post-submission annotations attached to this paper. Populated lazily; servers may return [] and require a separate query for annotations.",
      "items": { "$ref": "annotation.schema.json" }
    }
  }
}
